Tautulli:
  cap_add:
    - AUDIT_WRITE
    - CHOWN
    - DAC_OVERRIDE
    - FOWNER
    - FSETID
    - KILL
    - MKNOD
    - NET_BIND_SERVICE
    - NET_RAW
    - SETFCAP
    - SETGID
    - SETPCAP
    - SETUID
    - SYS_CHROOT
  cap_drop:
    - AUDIT_CONTROL
    - BLOCK_SUSPEND
    - DAC_READ_SEARCH
    - IPC_LOCK
    - IPC_OWNER
    - LEASE
    - LINUX_IMMUTABLE
    - MAC_ADMIN
    - MAC_OVERRIDE
    - NET_ADMIN
    - NET_BROADCAST
    - SYSLOG
    - SYS_ADMIN
    - SYS_BOOT
    - SYS_MODULE
    - SYS_NICE
    - SYS_PACCT
    - SYS_PTRACE
    - SYS_RAWIO
    - SYS_RESOURCE
    - SYS_TIME
    - SYS_TTY_CONFIG
    - WAKE_ALARM
  command:
    - ""
  container_name: Tautulli
  entrypoint:
    - /init
  environment:
    - TZ=Europe/Paris
    - PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
    - 'PS1=$(whoami)@$(hostname):$(pwd)\$ '
    - HOME=/root
    - TERM=xterm
    - TAUTULLI_DOCKER=True
  hostname: ac887120c988
  image: linuxserver/tautulli:latest
  ipc: private
  labels:
    build_version: 'Linuxserver.io version:- v2.5.6-ls14 Build-date:- 2020-10-15T12:29:45+00:00'
    maintainer: nemchik,thelamer
  logging:
    driver: json-file
    options: {}
  mac_address: 02:42:ac:11:00:05
  networks:
    bridge:
      aliases:
  ports:
    - 8181:8181/tcp
  restart: unless-stopped
  volumes:
    - /docker_data/Tautulli:/config
    - /docker_data/Tautulli-scripts:/00-scripts
    - b18263b31a3d2238c4d7bdb653e2e4b6286e6b34cf1bab135530f9941a8c99d0:/logs
    - /docker_data/Tautulli-newsletters:/app/tautulli/data/interfaces/custom-newsletters
